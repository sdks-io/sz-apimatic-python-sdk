"""seltzapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from seltzapi.api_helper import APIHelper
from seltzapi.models.fetch_error_1 import FetchError1


class FetchResult(object):
    """Implementation of the 'FetchResult' model.

    The outcome for one URL.

    Attributes:
        content_type (str): The origin's declared media type for the main document,
            without parameters.  Frequently unset, including on results that carry
            content. Never branch on it: `status` says whether a result carries
            content.
        error (FetchError1): The model property of type FetchError1.
        fetched_at (str): The model property of type str.
        final_url (str): The URL the content actually came from, after HTTP redirects
            and any client-side navigation. Equal to `requested_url` when nothing
            redirected.  Set on a result whose `status` is ERROR only when the
            failure happened after the origin answered, such as a page this service
            could not extract. Unset when the URL was never reached.
        http_status_code (int): The origin's HTTP status code for the main document.
            Unset when no network response was observed for the navigation.  An HTTP
            error status is not a fetch failure: this service renders the origin's
            error page, so a 404 arrives as an OK result carrying 404. On a result
            whose `status` is ERROR, this follows `final_url` - set only when the
            failure happened after the origin answered.
        markdown (str): The page's main content as Markdown, with boilerplate removed.
        requested_url (str): The URL this answers, echoed verbatim from the request
            -- byte for byte, never normalized, and never the post-redirect URL. This
            is the correlation key. Where redirects landed is `final_url`.
        status (FetchStatus2): The model property of type FetchStatus2.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "status": "status",
        "content_type": "content_type",
        "error": "error",
        "fetched_at": "fetched_at",
        "final_url": "final_url",
        "http_status_code": "http_status_code",
        "markdown": "markdown",
        "requested_url": "requested_url",
    }

    _optionals = [
        "content_type",
        "error",
        "fetched_at",
        "final_url",
        "http_status_code",
        "markdown",
        "requested_url",
    ]

    _nullables = [
        "content_type",
        "error",
        "fetched_at",
        "final_url",
        "http_status_code",
        "markdown",
    ]

    def __init__(
        self,
        status="ok",
        content_type=APIHelper.SKIP,
        error=APIHelper.SKIP,
        fetched_at=APIHelper.SKIP,
        final_url=APIHelper.SKIP,
        http_status_code=APIHelper.SKIP,
        markdown=APIHelper.SKIP,
        requested_url=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a FetchResult instance."""
        # Initialize members of the class
        if content_type is not APIHelper.SKIP:
            self.content_type = content_type
        if error is not APIHelper.SKIP:
            self.error = error
        if fetched_at is not APIHelper.SKIP:
            self.fetched_at = fetched_at
        if final_url is not APIHelper.SKIP:
            self.final_url = final_url
        if http_status_code is not APIHelper.SKIP:
            self.http_status_code = http_status_code
        if markdown is not APIHelper.SKIP:
            self.markdown = markdown
        if requested_url is not APIHelper.SKIP:
            self.requested_url = requested_url
        self.status = status

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        status =\
            dictionary.get("status")\
            if dictionary.get("status")\
                else "ok"
        content_type =\
            dictionary.get("content_type")\
            if "content_type" in dictionary.keys()\
                else APIHelper.SKIP
        if "error" in dictionary.keys():
            error =\
                FetchError1.from_dictionary(
                dictionary.get("error"))\
                if dictionary.get("error") else None
        else:
            error = APIHelper.SKIP
        fetched_at =\
            dictionary.get("fetched_at")\
            if "fetched_at" in dictionary.keys()\
                else APIHelper.SKIP
        final_url =\
            dictionary.get("final_url")\
            if "final_url" in dictionary.keys()\
                else APIHelper.SKIP
        http_status_code =\
            dictionary.get("http_status_code")\
            if "http_status_code" in dictionary.keys()\
                else APIHelper.SKIP
        markdown =\
            dictionary.get("markdown")\
            if "markdown" in dictionary.keys()\
                else APIHelper.SKIP
        requested_url =\
            dictionary.get("requested_url")\
            if dictionary.get("requested_url")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(status,
                   content_type,
                   error,
                   fetched_at,
                   final_url,
                   http_status_code,
                   markdown,
                   requested_url,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _content_type=(
            self.content_type
            if hasattr(self, "content_type")
            else None
        )
        _error=(
            self.error
            if hasattr(self, "error")
            else None
        )
        _fetched_at=(
            self.fetched_at
            if hasattr(self, "fetched_at")
            else None
        )
        _final_url=(
            self.final_url
            if hasattr(self, "final_url")
            else None
        )
        _http_status_code=(
            self.http_status_code
            if hasattr(self, "http_status_code")
            else None
        )
        _markdown=(
            self.markdown
            if hasattr(self, "markdown")
            else None
        )
        _requested_url=(
            self.requested_url
            if hasattr(self, "requested_url")
            else None
        )
        _status=self.status
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"content_type={_content_type!r}, "
            f"error={_error!r}, "
            f"fetched_at={_fetched_at!r}, "
            f"final_url={_final_url!r}, "
            f"http_status_code={_http_status_code!r}, "
            f"markdown={_markdown!r}, "
            f"requested_url={_requested_url!r}, "
            f"status={_status!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _content_type=(
            self.content_type
            if hasattr(self, "content_type")
            else None
        )
        _error=(
            self.error
            if hasattr(self, "error")
            else None
        )
        _fetched_at=(
            self.fetched_at
            if hasattr(self, "fetched_at")
            else None
        )
        _final_url=(
            self.final_url
            if hasattr(self, "final_url")
            else None
        )
        _http_status_code=(
            self.http_status_code
            if hasattr(self, "http_status_code")
            else None
        )
        _markdown=(
            self.markdown
            if hasattr(self, "markdown")
            else None
        )
        _requested_url=(
            self.requested_url
            if hasattr(self, "requested_url")
            else None
        )
        _status=self.status
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"content_type={_content_type!s}, "
            f"error={_error!s}, "
            f"fetched_at={_fetched_at!s}, "
            f"final_url={_final_url!s}, "
            f"http_status_code={_http_status_code!s}, "
            f"markdown={_markdown!s}, "
            f"requested_url={_requested_url!s}, "
            f"status={_status!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
